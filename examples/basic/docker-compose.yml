version: '3.8'

services:
  mongodb_basic:
    image: mongo:6
    container_name: mongo
    ports:
      - "27019:27017"
    volumes:
      - mongo_data_basic:/data/db
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example

volumes:
  mongo_data_basic:
